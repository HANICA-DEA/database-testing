#db.url=jdbc:mysql://127.0.0.1:3306/spotitube
db.url=jdbc:h2:mem:test;database_to_upper=false;MODE=MySQL
db.user=spotitube
db.password=spotitube123
# Zo werken H2 in-memory databases (http://www.h2database.com/html/features.html#in_memory_databases)
# Sometimes multiple connections to the same in-memory database are required.
# In this case, the database URL must include a name. Example: jdbc:h2:mem:db1.
# Accessing the same database using this URL only works within the same virtual
# machine and class loader environment.